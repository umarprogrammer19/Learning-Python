# WAP to count the number of students with the "A" grade in the followinf tupple
# ("C", "D", "A", "A", "B", "B", "A")

tup = ("C", "D", "A", "A", "B", "B", "A")
print(tup.count("A"))  # 3

# Store the above values in a list and sort them from A to D
list = ["C", "D", "A", "A", "B", "B", "A"]
list.sort()
print(list)
